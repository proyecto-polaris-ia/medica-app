import { getWccContactDetail } from '@/lib/wcc-contacts';
import { WccNotice } from '../../components';
export const dynamic='force-dynamic';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params; const data=await getWccContactDetail(id); return <main><h2 className="text-xl font-bold">Detalle de contacto</h2>{data.isConfiguredButUnavailable&&<WccNotice tone="warning">No se pudo leer el contacto.</WccNotice>}{data.contact?<section className="mt-4 rounded-2xl border bg-white p-5"><p className="font-semibold">{data.contact.displayName??data.contact.phoneE164}</p><p className="text-sm text-gray-600">{data.contact.phoneE164}</p><p className="text-sm text-gray-600">Paciente vinculado: {data.contact.linkedPatient?.fullName??'sin vínculo'}</p></section>:<p className="mt-4 text-sm text-gray-600">Contacto no encontrado.</p>}<section className="mt-6 grid gap-4 md:grid-cols-3"><div className="rounded-xl border bg-white p-4">Conversaciones: {data.conversations.length}</div><div className="rounded-xl border bg-white p-4">Escalaciones: {data.escalations.length}</div><div className="rounded-xl border bg-white p-4">Intents: {data.intents.length}</div></section></main>}
