import { createKnowledgeAction, updateKnowledgeAction } from './actions';
import type { WhatsAppKnowledgeEntry } from '@/types';
const statuses = ['draft','approved','archived'];
export function KnowledgeForm({ entry }: { entry?: WhatsAppKnowledgeEntry }) { const action = entry ? updateKnowledgeAction.bind(null, entry.id) : createKnowledgeAction; return <form action={action} className="space-y-4 rounded-2xl border bg-white p-5"><label className="block text-sm font-medium">Tema<input name="topic" defaultValue={entry?.topic} required className="mt-1 w-full rounded-lg border p-2"/></label><label className="block text-sm font-medium">Pregunta<textarea name="question" defaultValue={entry?.question} required className="mt-1 w-full rounded-lg border p-2"/></label><label className="block text-sm font-medium">Respuesta<textarea name="answer" defaultValue={entry?.answer} required rows={5} className="mt-1 w-full rounded-lg border p-2"/></label><label className="block text-sm font-medium">Tags<input name="tags" defaultValue={entry?.tags.join(', ')} className="mt-1 w-full rounded-lg border p-2"/></label><label className="block text-sm font-medium">Fuente<input name="source" defaultValue={entry?.source??''} className="mt-1 w-full rounded-lg border p-2"/></label><label className="block text-sm font-medium">Estado<select name="status" defaultValue={entry?.status??'draft'} className="mt-1 w-full rounded-lg border p-2">{statuses.map(s=><option key={s} value={s}>{s}</option>)}</select></label><button className="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Guardar</button></form> }
